# git-projects

History of git projects

## Installation

```bash
uv sync --frozen --group dev
```

## Usage

```bash
git-projects --help
git-projects hello
```

## Development

```bash
uv run invoke lint
uv run invoke test
uv run invoke format
```
